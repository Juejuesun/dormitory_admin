/**
 * @description 导出自定义配置
 **/
const config = {
  layout: 'vertical',
  donation: true,
  templateFolder: 'project',

  title: '适友阁',
  logo: false,
  header: 'fixed', //固定fixed 不固定noFixed
  // tagsView: true, //是否显示多标签页
  themeBar: true, //是否开启主题配置按钮
  // baseURL2: 'http://localhost:81' //默认的接口地址
  baseURL2: 'http://39.100.133.1:8443' //默认的接口地址
}
module.exports = config
